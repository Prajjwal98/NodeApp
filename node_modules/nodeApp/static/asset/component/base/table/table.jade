div.table
    table
        thead
            tr
                th(v-for="item in columns") {{ item.text }}
        tbody
    vpager(v-ref:pager, v-if="vispager", :total="total", :pagesize="vpagesize")