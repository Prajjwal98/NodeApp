div.radiogroup.clearfix
    div.field-label(v-if="isshowlabel" v-bind:style="labelstyle")
        span {{ labeltext }}：
    div
        template(v-for="item in items")
            label.item(:class="cls")
                input(type="radio", :value="item.value", v-model="val")
                span {{ item.text }}